Custom OAuth Social Login
OAuth токени + JWT
Authentication is implemented using a custom JWT-based social login.
The frontend (NextAuth) handles OAuth flows and provides provider access tokens to the backend, which verifies them and issues application-specific JWT tokens.
